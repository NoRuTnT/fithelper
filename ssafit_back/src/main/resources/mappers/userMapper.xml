<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.board.model.dao.UserDao">
	
	<select id="selectAll" resultType="User">
		select *
		from User
		
	</select>

	<insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
		insert into User (levelId, name,password, nickname, birth, email, phonenum,address,cash,sex)
		values (#{levelId},#{name},#{password}, #{nickname}, #{birth}, #{email}, #{phonenum},#{address},#{cash},#{sex})
	</insert>
	
	<delete id="deleteUser" parameterType="int">
		DELETE FROM User
		WHERE userId = #{userId}
	</delete>
	
	<update id="updateUser" parameterType="User">
		UPDATE User
		SET userId = #{userId}, password = #{password}, nickname = #{nickname}, birth = #{nickname}, email = #{email}, phonenum = #{phonenum}, address = #{address}
		WHERE userId = #{userId}
	</update>
	
	
	
	<select id="findByEmail" parameterType="String" resultType="User" >
		select * 
		from User
		where email = #{email}
		
	</select>
	
	
	
</mapper>